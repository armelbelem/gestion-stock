import pool from './app/lib/db.js'; async function run() { const [rows] = await pool.query('SELECT COUNT(*) as count FROM articles'); console.log('Total articles:', rows[0].count); const [cfaos] = await pool.query('SELECT COUNT(*) as count FROM articles WHERE name LIKE "%TOYOTA%"'); console.log('Toyota articles:', cfaos[0].count); const [clients] = await pool.query('SELECT * FROM clients'); console.log('Clients:', clients.map(c => c.name)); process.exit(0); } run();
